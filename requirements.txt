# Cumulative runtime deps. Populated per stage as the book introduces each library.
# Dev tooling lives in pyproject.toml under [project.optional-dependencies].dev
# Install with: pip install -r requirements.txt
