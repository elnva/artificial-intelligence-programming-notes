# Stage 01 — AI Development Tools

> Book chapter: **Chapter 2 — AI Development Tools**.
>
> Note on numbering: book Chapter 1 is the introduction; we begin our hands-on stages from Chapter 2 because that is where the tooling work starts. Stage numbers in this repo are independent of book chapter numbers, so this is `stage-01` even though the book calls it Chapter 2.

## Goal

By the end of this stage you will:

- Understand what the chapter calls "AI tools" and why each one exists.
- Have a working local environment ready to run the rest of the book's examples.
- Have hand-typed each introductory example yourself, so the muscle memory of writing the code reinforces the concepts.

The exact list of tools and concepts will be slotted in below as soon as the pages are shared.

## Prerequisites

- macOS with Python 3.10 available (`python3.10 --version`).
- This repo cloned, with the virtual environment created. If `.venv/` does not yet exist:
  ```
  python3.10 -m venv .venv
  source .venv/bin/activate
  pip install --upgrade pip
  ```
- A copy of the book on hand for reference.

## How to read the steps

Each step has four parts:

1. **Teach** — a short explanation of *what* the step is about and *why* it matters.
2. **Do** — the concrete action you take.
3. **Expected** — what you should see if it worked.
4. **Tell me when done** — your cue to ping Claude so the next step gets unlocked.

Do not skim ahead. The point is to absorb each idea before moving on.

## Steps

> Steps will be filled in incrementally once the chapter pages are shared. Below is the placeholder shape so you know what to expect.

### Step 1 — Placeholder

- **Teach**: (a sentence or two introducing the concept)
- **Do**: (the action the user takes)
- **Expected**: (what success looks like)
- **Tell me when done**: reply with "step 1 done" so Claude unlocks step 2.

### Step 2 — Placeholder

- **Teach**: …
- **Do**: …
- **Expected**: …
- **Tell me when done**: …

*(more steps will be added as the chapter content arrives)*

## Exercises

End-of-chapter exercises will be listed here. For each one:

- Create a file inside `stages/stage-01-ai-tools/exercises/` with a descriptive name.
- Type the solution yourself.
- Note any difficulty or interesting realisation in `notes.md`.

## Recap

Filled in at the end of the stage as a short bullet list of takeaways:

- (key idea 1)
- (key idea 2)
- (key idea 3)

When the recap is written, examples are typed, and exercises are attempted, change the row in [`docs/plan.md`](../plan.md) from `todo` / `in-progress` to `done`.
